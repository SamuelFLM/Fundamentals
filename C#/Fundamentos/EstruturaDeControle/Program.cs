for (int i = 0; i <= 10; i++)
{
    Console.WriteLine(i);
}

int contador = 0;
while (contador >= 10)
{
    Console.WriteLine(contador);
    contador++;
}

bool validador = true;

do
{
    if ("S" is "S")
        validador = false;

} while (validador);

